spring:
  application.name: contact-manager
  datasource:
    url: jdbc:mysql://localhost:3306/contact-manager
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
